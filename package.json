{
  "name": "agent-budget-experiment",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Give an agent a budget. Watch what it buys. — an autonomous agent with non-custodial cross-chain hands (Sippar) and a cap it cannot exceed.",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
